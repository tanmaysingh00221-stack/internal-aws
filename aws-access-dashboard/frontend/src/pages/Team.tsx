import { useEffect,useState } from 'react'; 
import { api } from '../lib/api'; 
type Member={id:string;name:string;email:string;role:string;active:boolean}; 
export function Team(){const [users,setUsers]=useState<Member[]>([]);useEffect(()=>{api<Member[]>('/admin/users').then(setUsers)},[]);return <><h1 className="text-3xl font-bold">Team access</h1><p className="mt-2 text-slate-400">Users and their assigned dashboard roles.</p><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{users.map(u=><div key={u.id} className="glass rounded-2xl p-5"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-orange-500 font-bold text-black">{u.name[0]}</div><h2 className="mt-4 font-semibold">{u.name}</h2><p className="text-sm text-slate-400">{u.email}</p><div className="mt-4 flex justify-between text-xs"><span className="rounded-full bg-slate-800 px-3 py-1">{u.role}</span><span className="text-emerald-400">{u.active?'Active':'Disabled'}</span></div></div>)}</div></>}
